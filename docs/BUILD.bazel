load("@rules_rust_mdbook//:defs.bzl", "mdbook", "mdbook_server")

filegroup(
    name = "srcs",
    srcs = glob(["**/*.md","**/*.yaml"]),
)

mdbook(
    name = "book",
    srcs = [
        ":srcs",
    ],
    book = ":book.toml",
)

mdbook_server(
    name = "server",
    book = ":book",
)
